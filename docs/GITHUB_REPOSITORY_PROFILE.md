# GitHub repository profile

Use the following copy when creating or updating the GitHub repository.

## Recommended repository name

`voxa`

## About — English

Rust-native real-time multimodal agent runtime with bounded graph execution and safe Rust, C++, Python, and TypeScript nodes.

## About — 简体中文

以 Rust 为核心的实时多模态 Agent Runtime，提供有界图执行以及安全的 Rust、C++、Python、TypeScript 节点接入。

GitHub's About field should normally use the English version for broader discovery. Both descriptions are intentionally short enough for the repository description field.

## Suggested topics

```text
rust
agent-runtime
agent-framework
multimodal
realtime
voice-agent
streaming
graph-runtime
workflow-engine
backpressure
ffi
cpp
pyo3
node-api
typescript
event-driven
rtc
ai-agents
```

## Website

Leave the Website field empty until project documentation or a dedicated site is publicly available. Do not point it at a private or temporary development address.

## Social preview copy

**Headline**

Voxa — Build real-time multimodal agents on one safe runtime

**Subheadline**

Bounded graph execution in Rust. Extensible with C++, Python, and TypeScript.

**中文标题**

Voxa — 用一套安全 Runtime 构建实时多模态 Agent

**中文副标题**

Rust 有界图执行，安全扩展 C++、Python 与 TypeScript 节点。

## Short introduction

### English

Voxa is a Rust-native runtime for building real-time multimodal agents as bounded processing graphs. It provides immutable frames, deterministic lifecycle and shutdown semantics, backpressure, control messaging, a versioned C ABI, language execution domains, Graph JSON, CLI tooling, and a local Studio foundation.

### 简体中文

Voxa 是一个以 Rust 为核心的实时多模态 Agent Runtime，通过有界处理图组织 Agent。它提供不可变 Frame、确定性生命周期与关闭语义、背压、控制消息、版本化 C ABI、多语言执行域、Graph JSON、CLI 和本地 Studio 基础能力。

## Public-release checklist

- Confirm the existing Apache-2.0 license and copyright attribution policy before the first release.
- Enable Issues and Discussions according to the intended contribution workflow.
- Enable private vulnerability reporting and add `SECURITY.md`.
- Add `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue forms, and a pull-request template.
- Configure branch protection and require the checked-in quality workflows.
- Add a social preview image using the copy above.
- Publish an initial pre-release tag only after package names and public APIs are reviewed.
