[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "voxa"
version = "0.1.0"
requires-python = ">=3.8"
description = "Safe Python bindings for the Voxa multimodal agent runtime"
readme = "README.md"
license = { text = "Apache-2.0" }
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Rust",
  "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/PiyotaHu/Voxa"
Documentation = "https://piyotahu.github.io/Voxa/nodes/python/"
Repository = "https://github.com/PiyotaHu/Voxa"
Issues = "https://github.com/PiyotaHu/Voxa/issues"

[tool.maturin]
module-name = "voxa._native"
python-source = "python"
features = ["extension-module"]

[tool.pytest.ini_options]
testpaths = ["tests"]
